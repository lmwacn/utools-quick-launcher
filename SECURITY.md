# 安全策略

## 报告安全问题

请优先使用 GitHub Security Advisory 私密报告安全问题，不要在公开 Issue 中披露可直接利用的命令执行、路径访问或导入绕过细节。

报告请包含受影响版本、操作系统、复现步骤和预期影响，但不要附带真实用户文件、访问令牌或其他敏感数据。

## 安全边界

- 命令资源以当前系统用户权限运行。
- 导入的命令默认不可信，首次执行必须由用户确认。
- 网页仅允许 HTTP/HTTPS。
- 自定义图标会缩放并压缩，原始图片不得超过 2 MB。
- preload 保持未压缩、未混淆，便于 uTools 审核和社区审计。
