# 变更记录

本项目遵循语义化版本。

## 2.1.0 - 2026-08-17

### 新增

- 命令工作目录、环境变量、后台/终端运行模式和跨平台常用模板。
- 收藏、最近使用、使用频率排序、自定义标签和中文拼音模糊搜索。
- 批量收藏、添加标签、导出和移入回收站。
- 导入数据预览，以及跳过、覆盖、保留两者三种冲突策略。
- 本机回收站和导入命令首次运行信任确认。
- 网页 favicon 获取和自定义图片自动缩放压缩。

### 工程与开源

- 采用 MIT License，并添加 SECURITY 和发布检查清单。
- 新增 1.x 数据回归样本、发布产物验证和 Windows/macOS/Linux CI。
- 发布产物自动包含 MIT LICENSE。

### 兼容

- 所有新同步字段保持可选；历史命令继续按全平台后台模式执行。
- 编辑未设置平台的旧版命令时继续保持全平台语义，不会静默改为当前系统专用。
- 使用统计和回收站仅保存在本机，不影响旧版同步文档。

### 修复

- 统一回收站容量提示与实际保留数量，并过滤损坏的本机状态条目。
- 清除筛选时同步重置标签，Windows 和 Linux 显示正确的 Ctrl 快捷键提示。

## 2.0.0 - 2026-08-17

### 新增

- 使用 React 19.2.3、Vite 和 TypeScript 重构整个插件。
- 统一搜索、类型数量、空状态和响应式卡片网格。
- 失效路径提示、删除撤销和错误反馈。
- 浅色/深色主题、键盘导航和减少动画支持。
- 自动化测试、类型检查、ESLint 和 CI 工作流。
- macOS zsh、Windows CMD 和 Linux Shell 命令执行及平台限制。

### 兼容

- 保留旧插件标识 `zqbyq8zn`。
- 继续读写 `launcher-items.data`。
- 继续使用 `item_${id}` 动态指令。
- 保留历史资源字段并兼容 `url`、`cmd` 和 `command` 别名。
- 未设置 `platform` 的历史命令继续在所有系统显示和运行。

### 优化

- 卡片与图标改为紧凑密度，同一窗口可显示更多资源。
- 恢复经典蓝色纸飞机 Logo，并为文件、文件夹、网页和命令绘制统一 SVG 图标。
- 本地文件和文件夹路径支持直接修改或重新选择，保存时校验路径及类型。

### 安全和稳定性

- 网页启动限制为 HTTP/HTTPS 协议。
- 新上传的本地图标限制为 300 KB。
- 保存前检查 uTools 同步文档体积。
- 打开资源和执行命令时返回明确结果。

## 1.0.4

- 历史发布版本，源码已遗失，保留 ASAR 发布产物作为兼容性依据。
